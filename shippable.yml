# Language setting
language: python

# Version number
python:
  - 2.7

build:
  ci:
    - python --version
    - touch cache.txt
    - rm -rf /tmp/cache
    - mkdir -p /tmp/cache
    - cp cache.txt /tmp/cache
  cache: true
  cache_dir_list:
    - /tmp/cache